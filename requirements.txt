# LTW Video Splitter Pro - Requirements
# 100% local / free. No cloud API keys required.
# Python 3.10+ recommended.

# ---- Core video / imaging ----
moviepy>=2.0.0,<3.0.0
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.9.0
scikit-image>=0.22.0
scipy>=1.11.0
scikit-learn>=1.3.0
tqdm>=4.66.0
pydub>=0.25.1
ffmpeg-python>=0.2.0

# ---- GUI ----
customtkinter>=5.2.0
tkinterdnd2>=0.4.0

# ---- Local AI: transcription ----
faster-whisper>=1.0.0

# ---- Local AI: face / subject tracking (required) ----
mediapipe>=0.10.11

# ---- Config, validation, logging ----
pydantic>=2.6.0
pydantic-settings>=2.2.0
PyYAML>=6.0.1
rich>=13.7.0
structlog>=24.1.0

# ---- Optional: local LLM (via Ollama HTTP API) ----
# Install Ollama separately from https://ollama.com for smart title/description generation.
# No Python package needed - we call the HTTP API directly with stdlib.

# ---- Audio / music analysis (used by beat_detector) ----
librosa>=0.10.0
